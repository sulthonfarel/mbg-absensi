rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes (for internal school use)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
